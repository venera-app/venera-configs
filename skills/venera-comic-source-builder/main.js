const fs = require('fs');
const path = require('path');
const { exec } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
    }

    async start() {
        await this.collectBasicInfo();
        await this.runTests();
        await this.generateSource();
        await this.showReport();
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = exec(`python3 "${pythonScript}" ${args.join(' ')}`, (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `                let src = el.getAttribute("${comicConfig.imageAttr}")\n` +
            `                if (src) images.push(src)\n` +
            `            })\n` +
            `            return { images: images }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{COMIC_SECTION\}\}/g, comicCode);
    }

    async showReport() {
        console.log('\n' + '='.repeat(60));
        console.log('          漫画源生成报告');
        console.log('='.repeat(60));
        
        this.report.forEach(item => {
            let prefix = '  ';
            switch (item.type) {
                case 'success': prefix = '  ✅ '; break;
                case 'error': prefix = '  ❌ '; break;
                case 'warning': prefix = '  ⚠️ '; break;
                case 'info': prefix = '  ℹ️ '; break;
            }
            console.log(prefix + item.message);
        });
        
        console.log('='.repeat(60));
        if (this.outputPath) {
            console.log(`\n生成的源文件: ${this.outputPath}`);
        }
    }
}

if (require.main === module) {
    const builder = new ComicSourceBuilder();
    builder.start().catch(err => {
        console.error('生成失败:', err.message);
        process.exit(1);
    });
}

module.exports = ComicSourceBuilder;const fs = require('fs');
const pathconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report =const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries =const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        }const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies =const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const depconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成'const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return newconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageNameconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.reportconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${errorconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ typeconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = awaitconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.keyconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：',const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', trueconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() ===const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.splitconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.nameconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key =const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = falseconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdoutconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readlineconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error)const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt}const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries)const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.dconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeoutconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.pushconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args =const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.nameconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.versionconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'falseconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFileconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message:const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines =const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWithconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.sliceconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        thisconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    }const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warningconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERRORconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    asyncconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, thisconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, thisconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, thisconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, thisconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCodeconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = pathconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig =const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\nconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\nconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 20const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document =const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover:const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}")const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return {const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode +=const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SEconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode)const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig =const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n`const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comicconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\nconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\nconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) returnconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode =const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n`const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name:const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n`const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.labelconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            pageconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.valueconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\nconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n`const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORYconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        constconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", idconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n`const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelectorconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.textconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfigconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters =const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n`const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n`const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title:const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n`const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.getconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throwconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            documentconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `                let src = el.getAttribute("${comicConfig.imageAttr}")\n` +
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `                let src = el.getAttribute("${comicConfig.imageAttr}")\n` +
            `                if (src) images.push(src)\n` +
            `            })\const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `                let src = el.getAttribute("${comicConfig.imageAttr}")\n` +
            `                if (src) images.push(src)\n` +
            `            })\n` +
            `            return { images: images }\n` +
            `const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `                let src = el.getAttribute("${comicConfig.imageAttr}")\n` +
            `                if (src) images.push(src)\n` +
            `            })\n` +
            `            return { images: images }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `                let src = el.getAttribute("${comicConfig.imageAttr}")\n` +
            `                if (src) images.push(src)\n` +
            `            })\n` +
            `            return { images: images }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{COMIC_SECTION\}\}/g, comicCode);
    }

    async showconst fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `                let src = el.getAttribute("${comicConfig.imageAttr}")\n` +
            `                if (src) images.push(src)\n` +
            `            })\n` +
            `            return { images: images }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{COMIC_SECTION\}\}/g, comicCode);
    }

    async showReport() {
        console.log('\n' + '='.repeat(60));
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `                let src = el.getAttribute("${comicConfig.imageAttr}")\n` +
            `                if (src) images.push(src)\n` +
            `            })\n` +
            `            return { images: images }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{COMIC_SECTION\}\}/g, comicCode);
    }

    async showReport() {
        console.log('\n' + '='.repeat(60));
        console.log('          漫画源生成报告');
        console.log('='.repeat(const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `                let src = el.getAttribute("${comicConfig.imageAttr}")\n` +
            `                if (src) images.push(src)\n` +
            `            })\n` +
            `            return { images: images }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{COMIC_SECTION\}\}/g, comicCode);
    }

    async showReport() {
        console.log('\n' + '='.repeat(60));
        console.log('          漫画源生成报告');
        console.log('='.repeat(60));
        
        this.report.forEach(item => {
            let prefix = '  ';const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `                let src = el.getAttribute("${comicConfig.imageAttr}")\n` +
            `                if (src) images.push(src)\n` +
            `            })\n` +
            `            return { images: images }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{COMIC_SECTION\}\}/g, comicCode);
    }

    async showReport() {
        console.log('\n' + '='.repeat(60));
        console.log('          漫画源生成报告');
        console.log('='.repeat(60));
        
        this.report.forEach(item => {
            let prefix = '  ';
            switch (item.type) {
                case 'success': prefix = '  ✅ 'const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `                let src = el.getAttribute("${comicConfig.imageAttr}")\n` +
            `                if (src) images.push(src)\n` +
            `            })\n` +
            `            return { images: images }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{COMIC_SECTION\}\}/g, comicCode);
    }

    async showReport() {
        console.log('\n' + '='.repeat(60));
        console.log('          漫画源生成报告');
        console.log('='.repeat(60));
        
        this.report.forEach(item => {
            let prefix = '  ';
            switch (item.type) {
                case 'success': prefix = '  ✅ '; break;
                case 'error': prefix = '  ❌ '; break;
const fs = require('fs');
const path = require('path');
const { exec, execFile } = require('child_process');

class ComicSourceBuilder {
    constructor() {
        this.userInput = {};
        this.currentStep = 0;
        this.testResults = {};
        this.report = [];
        this.maxRetries = 3;
        this.retryDelay = 2000;
    }

    async start() {
        try {
            await this.ensureDependencies();
            await this.collectBasicInfo();
            await this.runTestsWithRetry();
            await this.generateSource();
            await this.showReport();
        } catch (error) {
            this.report.push({ type: 'error', message: `生成失败: ${error.message}` });
            await this.showReport();
            process.exit(1);
        }
    }

    async ensureDependencies() {
        this.report.push({ type: 'info', message: '检查并安装依赖...' });
        
        const dependencies = ['requests', 'beautifulsoup4', 'fake-useragent'];
        
        for (const dep of dependencies) {
            const installed = await this.isPackageInstalled(dep);
            if (!installed) {
                this.report.push({ type: 'info', message: `安装依赖: ${dep}` });
                await this.installPackage(dep);
            }
        }
        
        this.report.push({ type: 'success', message: '依赖检查完成' });
    }

    async isPackageInstalled(packageName) {
        return new Promise((resolve) => {
            exec('python3 -c "import ' + packageName.replace('-', '_') + '"', (error) => {
                resolve(!error);
            });
        });
    }

    async installPackage(packageName) {
        return new Promise((resolve, reject) => {
            const child = exec(`pip install ${packageName}`, (error) => {
                if (error) {
                    this.report.push({ type: 'error', message: `安装 ${packageName} 失败: ${error.message}` });
                    reject(error);
                } else {
                    this.report.push({ type: 'success', message: `安装 ${packageName} 成功` });
                    resolve();
                }
            });

            child.stdout.on('data', (data) => {
                this.report.push({ type: 'info', message: `安装日志: ${data.trim()}` });
            });

            child.stderr.on('data', (data) => {
                this.report.push({ type: 'warning', message: `安装警告: ${data.trim()}` });
            });
        });
    }

    async collectBasicInfo() {
        this.report.push({ type: 'info', message: '开始收集漫画源基本信息...' });
        
        this.userInput.url = await this.prompt('请输入漫画网站URL（必填）：');
        this.userInput.name = await this.prompt('请输入网站名称（可选，默认从URL提取）：', null, true);
        this.userInput.key = await this.prompt('请输入源Key（可选，默认小写网站名）：', null, true);
        this.userInput.version = await this.prompt('请输入版本号（可选，默认1.0.0）：', '1.0.0', true);
        this.userInput.needLogin = await this.prompt('是否需要登录？（y/n，默认n）：', 'n', true).then(v => v.toLowerCase() === 'y');
        this.userInput.backupDomains = await this.prompt('请输入备用域名（可选，逗号分隔）：', '', true).then(v => v ? v.split(',').map(s => s.trim()) : []);

        if (!this.userInput.name) {
            this.userInput.name = this.extractNameFromUrl(this.userInput.url);
        }
        if (!this.userInput.key) {
            this.userInput.key = this.userInput.name.toLowerCase().replace(/[^a-z0-9]/g, '_');
        }

        this.report.push({ type: 'success', message: `已收集信息：${this.userInput.name} (${this.userInput.key})` });
    }

    extractNameFromUrl(url) {
        try {
            const u = new URL(url);
            return u.hostname.replace(/^(www\.)?/, '').replace(/\.[a-z]+$/, '');
        } catch {
            return 'comic_source';
        }
    }

    async prompt(message, defaultValue = null, allowEmpty = false) {
        return new Promise((resolve) => {
            const readline = require('readline').createInterface({
                input: process.stdin,
                output: process.stdout
            });
            readline.question(message, (answer) => {
                readline.close();
                if (answer.trim() === '' && allowEmpty) {
                    resolve(defaultValue);
                } else {
                    resolve(answer.trim() || defaultValue);
                }
            });
        });
    }

    async runTestsWithRetry() {
        for (let attempt = 1; attempt <= this.maxRetries; attempt++) {
            try {
                await this.runTests();
                return;
            } catch (error) {
                this.report.push({ type: 'warning', message: `第 ${attempt} 次测试失败: ${error.message}` });
                if (attempt < this.maxRetries) {
                    this.report.push({ type: 'info', message: `等待 ${this.retryDelay / 1000} 秒后重试...` });
                    await this.delay(this.retryDelay);
                } else {
                    throw new Error(`测试失败，已重试 ${this.maxRetries} 次`);
                }
            }
        }
    }

    delay(ms) {
        return new Promise(resolve => setTimeout(resolve, ms));
    }

    async runTests() {
        this.report.push({ type: 'info', message: '\n开始运行测试...' });

        const pythonScript = path.join(__dirname, 'comic_source_tester.py');
        const args = [
            '--url', this.userInput.url,
            '--name', this.userInput.name,
            '--key', this.userInput.key,
            '--version', this.userInput.version,
            '--need-login', this.userInput.needLogin ? 'true' : 'false',
            '--backup-domains', this.userInput.backupDomains.join(',')
        ];

        return new Promise((resolve, reject) => {
            const child = execFile('python3', [pythonScript, ...args], (error, stdout, stderr) => {
                if (error) {
                    this.report.push({ type: 'error', message: `测试失败: ${error.message}` });
                    reject(error);
                } else {
                    try {
                        this.testResults = JSON.parse(stdout);
                        resolve();
                    } catch {
                        this.report.push({ type: 'error', message: '解析测试结果失败' });
                        reject(new Error('解析失败'));
                    }
                }
            });

            child.stdout.on('data', (data) => {
                const lines = data.toString().split('\n');
                lines.forEach(line => {
                    if (line.startsWith('[INFO]')) {
                        this.report.push({ type: 'info', message: line.slice(6) });
                    } else if (line.startsWith('[SUCCESS]')) {
                        this.report.push({ type: 'success', message: line.slice(10) });
                    } else if (line.startsWith('[WARNING]')) {
                        this.report.push({ type: 'warning', message: line.slice(10) });
                    } else if (line.startsWith('[ERROR]')) {
                        this.report.push({ type: 'error', message: line.slice(8) });
                    }
                });
            });
        });
    }

    async generateSource() {
        this.report.push({ type: 'info', message: '\n开始生成漫画源文件...' });

        const template = this.loadTemplate();
        let sourceCode = template;

        sourceCode = sourceCode.replace(/{{NAME}}/g, this.userInput.name);
        sourceCode = sourceCode.replace(/{{KEY}}/g, this.userInput.key);
        sourceCode = sourceCode.replace(/{{VERSION}}/g, this.userInput.version);
        sourceCode = sourceCode.replace(/{{URL}}/g, this.userInput.url);

        sourceCode = this.fillExploreSection(sourceCode);
        sourceCode = this.fillSearchSection(sourceCode);
        sourceCode = this.fillCategorySection(sourceCode);
        sourceCode = this.fillComicSection(sourceCode);

        const outputPath = path.join(process.cwd(), `${this.userInput.key}.js`);
        fs.writeFileSync(outputPath, sourceCode);

        this.report.push({ type: 'success', message: `漫画源文件已生成: ${outputPath}` });
        this.outputPath = outputPath;
    }

    loadTemplate() {
        return fs.readFileSync(path.join(__dirname, 'template.js'), 'utf8');
    }

    fillExploreSection(sourceCode) {
        if (!this.testResults.explore) return sourceCode;
        
        const exploreConfig = this.testResults.explore;
        let exploreCode = `    explore = [\n`;
        
        exploreConfig.sections.forEach(section => {
            exploreCode += `        {\n`;
            exploreCode += `            title: "${section.title}",\n`;
            exploreCode += `            type: "${section.type || 'multiPartPage'}",\n`;
            exploreCode += `            load: async (page) => {\n`;
            exploreCode += `                let res = await Network.get("${section.url}")\n`;
            exploreCode += `                if (res.status !== 200) throw "请求失败"\n`;
            exploreCode += `                let document = new HtmlDocument(res.body)\n`;
            exploreCode += `                let comics = []\n`;
            exploreCode += `                document.querySelectorAll("${section.selector}").forEach(el => {\n`;
            exploreCode += `                    comics.push(new Comic({\n`;
            exploreCode += `                        id: el.getAttribute("${section.idAttr}"),\n`;
            exploreCode += `                        title: el.querySelector("${section.titleSelector}")?.text || "",\n`;
            exploreCode += `                        cover: el.querySelector("${section.coverSelector}")?.getAttribute("${section.coverAttr}") || "",\n`;
            exploreCode += `                    }))\n`;
            exploreCode += `                })\n`;
            exploreCode += `                return { "${section.key}": comics }\n`;
            exploreCode += `            }\n`;
            exploreCode += `        }\n`;
        });
        
        exploreCode += `    ]\n`;
        
        return sourceCode.replace(/\{\{EXPLORE_SECTION\}\}/g, exploreCode);
    }

    fillSearchSection(sourceCode) {
        if (!this.testResults.search) return sourceCode;
        
        const searchConfig = this.testResults.search;
        const searchCode = `    search = {\n` +
            `        load: async (keyword, options, page) => {\n` +
            `            let url = "${searchConfig.url}".replace("{{keyword}}", encodeURIComponent(keyword))\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let comics = []\n` +
            `            document.querySelectorAll("${searchConfig.selector}").forEach(el => {\n` +
            `                comics.push(new Comic({\n` +
            `                    id: el.getAttribute("${searchConfig.idAttr}"),\n` +
            `                    title: el.querySelector("${searchConfig.titleSelector}")?.text || "",\n` +
            `                    cover: el.querySelector("${searchConfig.coverSelector}")?.getAttribute("${searchConfig.coverAttr}") || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return { comics: comics, maxPage: 10 }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{SEARCH_SECTION\}\}/g, searchCode);
    }

    fillCategorySection(sourceCode) {
        if (!this.testResults.category) return sourceCode;
        
        const categoryConfig = this.testResults.category;
        let categoryCode = `    category = {\n` +
            `        title: "分类",\n` +
            `        parts: [\n`;
        
        categoryConfig.categories.forEach(cat => {
            categoryCode += `            {\n` +
                `                name: "${cat.name}",\n` +
                `                type: "fixed",\n` +
                `                categories: [\n`;
            cat.items.forEach(item => {
                categoryCode += `                    {\n` +
                    `                        label: "${item.label}",\n` +
                    `                        target: {\n` +
                    `                            page: "category",\n` +
                    `                            attributes: { category: "${item.value}" }\n` +
                    `                        }\n` +
                    `                    },\n`;
            });
            categoryCode += `                ]\n` +
                `            },\n`;
        });
        
        categoryCode += `        ]\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{CATEGORY_SECTION\}\}/g, categoryCode);
    }

    fillComicSection(sourceCode) {
        if (!this.testResults.comic) return sourceCode;
        
        const comicConfig = this.testResults.comic;
        const comicCode = `    comic = {\n` +
            `        loadInfo: async (id) => {\n` +
            `            let url = "${comicConfig.infoUrl}".replace("{{id}}", id)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let title = document.querySelector("${comicConfig.titleSelector}")?.text || ""\n` +
            `            let cover = document.querySelector("${comicConfig.coverSelector}")?.getAttribute("${comicConfig.coverAttr}") || ""\n` +
            `            let author = document.querySelector("${comicConfig.authorSelector}")?.text || ""\n` +
            `            let description = document.querySelector("${comicConfig.descSelector}")?.text || ""\n` +
            `            let chapters = []\n` +
            `            document.querySelectorAll("${comicConfig.chapterSelector}").forEach(el => {\n` +
            `                chapters.push(new Chapter({\n` +
            `                    id: el.getAttribute("${comicConfig.chapterIdAttr}"),\n` +
            `                    title: el.querySelector("${comicConfig.chapterTitleSelector}")?.text || "",\n` +
            `                }))\n` +
            `            })\n` +
            `            return new ComicDetails({\n` +
            `                id: id,\n` +
            `                title: title,\n` +
            `                cover: cover,\n` +
            `                author: author,\n` +
            `                description: description,\n` +
            `                chapters: chapters.reverse()\n` +
            `            })\n` +
            `        },\n` +
            `        loadEp: async (comicId, epId) => {\n` +
            `            let url = "${comicConfig.epUrl}".replace("{{comicId}}", comicId).replace("{{epId}}", epId)\n` +
            `            let res = await Network.get(url)\n` +
            `            if (res.status !== 200) throw "请求失败"\n` +
            `            let document = new HtmlDocument(res.body)\n` +
            `            let images = []\n` +
            `            document.querySelectorAll("${comicConfig.imageSelector}").forEach(el => {\n` +
            `                let src = el.getAttribute("${comicConfig.imageAttr}")\n` +
            `                if (src) images.push(src)\n` +
            `            })\n` +
            `            return { images: images }\n` +
            `        }\n` +
            `    }\n`;
        
        return sourceCode.replace(/\{\{COMIC_SECTION\}\}/g, comicCode);
    }

    async showReport() {
        console.log('\n' + '='.repeat(60));
        console.log('          漫画源生成报告');
        console.log('='.repeat(60));
        
        this.report.forEach(item => {
            let prefix = '  ';
            switch (item.type) {
                case 'success': prefix = '  ✅ '; break;
                case 'error': prefix = '  ❌ '; break;
                case 'warning': prefix = '  ⚠️ '; break;
                case 'info':